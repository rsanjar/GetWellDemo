@model Patient

<script type="text/javascript" defer>
    $(document).ready(function() {
        //Datetimepicker plugin
        $('.datetimepicker').bootstrapMaterialDatePicker({
            clearButton: true,
            weekStart: 1,
            language: 'ru',
            format: 'DD-MM-YYYY',
            date: true,
            time: false
            //maxDate: Date.now
        });

        $('.timepicker').bootstrapMaterialDatePicker({
            format: 'HH:mm',
            clearButton: true,
            date: false
        });

        $('#Profile_CityID').on('change',
            function() {
                loadRegions(this.value);
            });

        if('@Model.PatientProfile.CityID' * 1 > 0)
            loadRegions(@Model.PatientProfile.CityID, @Model.PatientProfile.RegionID);
    });

    function loadRegions(cityId, selectedId) {
        ajaxLoadSelect('/api/region/getall/', cityId, '#Profile_RegionID', 'Регион', selectedId);
    }
</script>